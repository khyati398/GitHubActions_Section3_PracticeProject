name: Issues Workflow
on: 
    issues:
        types: [opened, edited]
jobs:
    Issue-Details:
        runs-on: ubuntu-latest
        steps:
            - name: Output of issue
              run: echo "Title - ${{ github.issue.title }}, Message - ${{ github.issue.body }} & Username - ${{ github.issue.user.login }}"
